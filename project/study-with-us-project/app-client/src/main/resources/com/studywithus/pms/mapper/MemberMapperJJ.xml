<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--  select할 컬럼 정리하기 -->

<mapper namespace="MemberMapperJJ">
  
  <resultMap type="Member" id="MemberMapJJ">
    <id column="member_no" property="no"/> 
    <result column="name" property="name"/>
    <result column="email" property="email"/>
    <result column="password" property="password"/>
    <result column="phone_number" property="phoneNumber"/>
    <result column="join_date" property="registeredDate"/>
    <result column="status" property="status"/>
    <result column="last_date" property="lastDate"/>
    <result column="access_level" property="userAccessLevel"/>
    
  </resultMap>
  
  <select id="findAll" resultMap="MemberMapJJ">
    select 
      member_no,
      name,
      email,
      phone_number,
      join_date,
      status,
      last_date,
      access_level
    from 
      member 
    order by 
      name asc
  </select>
  
  <select id="findByNo" resultMap="MemberMapJJ" parameterType="int">
    select 
      member_no,
      name,
      email,
      phone_number,
      join_date,
      status,
      last_date,
      access_level
    from 
      member 
    where 
      member_no=#{no}
  </select>
  
  <select id="findByEmail" resultMap="MemberMapJJ" parameterType="string">
    select 
      ?
    from 
      member 
    where 
      email=#{email}
  </select>

	<select id="findByName" resultMap="MemberMapJJ" parameterType="map">
	  select 
	    member_no,
      name,
      email,
      phone_number,
      join_date,
      status,
      last_date,
      access_level
	  from member
	  where name=#{name}
	   </select>
	   
	 <select id="findMemberByNamePhoneNumber" resultMap="MemberMapJJ" parameterType="map">
	    select 
	      member_no,
	      name,
	      email,
	      phone_number,
	      join_date,
	      status,
	      last_date,
	      access_level
	    from member
	    where name=#{name} and email=#{email} and phone_number=#{phoneNumber})
	  </select>
	  
	   <select id="findMember" resultMap="MemberMap" parameterType="map">
      select 
        member_no,
        name,
        email,
        phone_number,
        join_date,
        status,
        last_date,
        access_level
      from member
      where name=#{name} and email=#{email} and password=password(#{password})
    </select>
    
     <select id="findMemberByEmailPassword" resultMap="MemberMap" parameterType="map">
      select 
        member_no,
        name,
        email,
        phone_number,
        join_date,
        status,
        last_date,
        access_level
      from member
      where email=#{email} and password=password(#{password})
    </select>
    
  
		<insert id="insert" parameterType="member">
		 insert into member(
	      name,
	      email,
	      password,
	      phone_number,
	      join_date,
	      status,
	      last_date,
	      access_level)
		  values(#{name},#{email},password(#{password}),#{phoneNumber},#{registeredDate},#{status},#{lastDate},#{userAccessLevel})
		</insert>
		  
		<update id="update" parameterType="member">
		 update pms_member set
	      name=#{name},
	      email=#{email},
	      password=password(#{password}),
	      phone_number=#{phoneNumber},
	      join_date=#{registeredDate},
	      status=#{status},
	      last_date=#{lastDate},
	      access_level=#{userAccessLevel}
	   where member_no=#{no}  
	  </update>
	  
	  <delete id="delete" parameterType="int">
	    delete from member
	    where member_no=#{no}
	  </delete>
            
</mapper>







